services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - db-data:/app/db
    environment:
      - DJANGO_SETTINGS_MODULE=expense_tracker.settings

volumes:
  db-data:
